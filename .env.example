# 复制成 .env 后填写（或直接 npm run set-password 自动生成）
PORT=3000
HOST=127.0.0.1
NODE_ENV=production

# bcrypt 密码散列，用 npm run set-password 生成，不要手填
PASSWORD_HASH=
# 会话签名密钥，随机字符串即可
SESSION_SECRET=
# 登录保持天数
SESSION_DAYS=30
